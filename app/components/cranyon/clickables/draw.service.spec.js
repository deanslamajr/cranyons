import DrawService from './draw.service';

describe('draw.service', () => {
  xcontext('createSVGDocument()', () => {
    xit('should create and return a new SVG document associated with the passed id', () => {

    });
  });

  xcontext('createClickable()', () => {
    xit('should return a new SVG clickable if the passed clickable datastructure is of type SVG', () => {

    });

    xit('should throw an Error if the passed clickable datastructure does not match any provided types', () => {

    });
  });

  xcontext('createPathClickable()', () => {
    xit(`should create and return the properly constructed SVG 'set'`, () => {

    });
  });

  xcontext('generateShape()', () => {
    xit('should return a properly formatted path string', () => {

    });
  });

  xcontext('appendSnippetToPath()', () => {
    xit('should return a properly formatted snippet string', () => {

    });
  });
});
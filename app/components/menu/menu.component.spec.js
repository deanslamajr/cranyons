import { MenuCtrl } from './menu.component';

describe('menu.component', () => {
  xcontext('constructor', () => {
    xit('should initialize isActive to false', () => {

    });
  });

  xcontext('$postLink()', () => {
    xit(`should add a 'click' listener to the menu element`, () => {

    });

    // possibly move this functionality into its own method
    xit('should change the value of this.isActive', () => {

    });
  });
});